#include <string.h>
#include "main.h"

/**
 *_strlen_recursion - print the length of a string
 *@s: is the string
 * Return: return the length
 */
int _strlen_recursion(char *s)
{
return (strlen(s));

}
